import styled from 'styled-components';

const Container = styled.div`
  position: fixed;
  z-index: 99999;
  bottom: 15px;
  left: 15px;

  width: calc(100% - 30px);
`;

export default {
  Container,
};
